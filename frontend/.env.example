# Where the browser sends API calls.
# Leave as /api in development so Vite's proxy handles it (no CORS setup needed).
VITE_API_BASE_URL=/api

# Only used by the Vite dev proxy — the origin Django is served from.
VITE_API_PROXY_TARGET=http://127.0.0.1:8000
